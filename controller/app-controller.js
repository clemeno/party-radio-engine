'use strict'

// const path = require('path')

const appController = async fastify => {
  // fastify.route({
  //   method: 'GET',
  //   url: '/app_error_log',
  //   handler: async reply => reply.sendFile(path.join(__dirname, '..', 'var', 'logs', 'error.log'))
  // })
}

module.exports = { appController }
